<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.robben.bbs.mapper.ConfessionWallMapper">    <select id="getAllConfessionWall"            resultType="com.robben.bbs.pojo.ConfessionWall">        select        a.id,a.content,a.image1,a.image2,a.image3,b.userName,b.introduction,b.avatar        from        confessionWall        a,oridinaryUser b         where        id=a.id and b.userId=a.userId    </select>    <select id="getConfessionWallByUserId" resultType="com.robben.bbs.pojo.ConfessionWall">        select * from confessionWall where userId=#{userId}    </select>    <insert id="postConfessionWall" >        insert into confessionWall        <trim prefix="(" suffix=")" suffixOverrides=",">            userId,content,            <if test="image1!=null">                image1,            </if>            <if test="image2!=null">                image2,            </if>            <if test="image3!=null">                image3,            </if>        </trim>        <trim prefix="values(" suffix=")" suffixOverrides=",">            #{userId},#{content},            <if test="image1!=null">                #{image1},            </if>            <if test="image2!=null">                #{image2},            </if>            <if test="image3!=null">                #{image3},            </if>        </trim>    </insert>    <delete id="deleteConfessionWallById">        delete from confessionWall where  id=#{id}    </delete>    <update id="updateConfessionWall"            parameterType="com.robben.bbs.pojo.ConfessionWall">        update confessionWall set content=#{content} where id=#{id}    </update></mapper>