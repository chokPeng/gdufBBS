package com.robben.bbs.service.impl;import com.robben.bbs.service.RedisService;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.autoconfigure.data.redis.RedisProperties;import org.springframework.data.redis.connection.lettuce.LettucePoolingClientConfiguration;import org.springframework.data.redis.core.RedisTemplate;import org.springframework.data.redis.core.ValueOperations;import org.springframework.data.redis.core.script.DefaultRedisScript;import org.springframework.stereotype.Service;import java.util.Collections;import java.util.concurrent.TimeUnit;@Servicepublic class RedisServiceImpl implements RedisService {    private RedisTemplate<String,Object> redisTemplate;    final String command="setbit test 0 1;setbit test 1 1;bitcount test";    @Autowired    public RedisServiceImpl(RedisTemplate<String,Object> redisTemplate){        this.redisTemplate=redisTemplate;    }    @Override    public Long savePostLike(Long postId,Integer userNo) {        redisTemplate.opsForValue().setBit(postId.toString(),userNo,true);        return null;    }    @Override    public Long deletePostLike(Long postId,Integer userNo) {        redisTemplate.opsForValue().setBit(postId.toString(),userNo,false);        return redisTemplate.opsForValue().size(postId.toString());    }    @Override    public Boolean saveOnlineUser(Long userId) {        ValueOperations<String,Object> ops = redisTemplate.opsForValue();        return ops.setBit("onlineUser",userId,true);    }    @Override    public Integer getOnlineUser() {        //I want to use Redis command "bitcount 'onlineUser'" here to count the number of online users        return null;    }    @Override    public Boolean deleteOnlineUser(Integer userNo) {        return null;    }}