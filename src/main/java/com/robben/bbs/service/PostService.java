package com.robben.bbs.service;import com.robben.bbs.pojo.Post;import org.springframework.web.multipart.MultipartFile;import java.io.IOException;import java.util.List;public interface PostService {    List getTopic();    List getAllPosts();    Post getPostByPostId(Long postId);    Long savePost(Long posterId, String content, String title, String topic);    Integer deletePostById(Long postId);    Integer updatePost(Long postId, String topic, String title, String content, Integer readingTime);}