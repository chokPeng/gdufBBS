<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.robben.bbs.mapper.RelationshipMapper">    <select id="getFollower" resultType="java.util.HashMap">        select userId,username,avatar from ordinaryUser where userId        in(select followerId from relationship where followeeId=#{followeeId})    </select>    <select id="getFollowee" resultType="java.util.HashMap">        select userId,username,avatar from ordinaryUser where userId        in(select followeeId from relationship where followerId=#{followerId})    </select>    <insert id="saveFollower">        insert into relationship(followeeId,followerId) values(#{followeeId},#{followerId})    </insert>    <delete id="deleteFollower">        delete from relationship where followeeId=#{followeeId} and        followerId=#{followerId}    </delete>    <select id="getFollowerNumber" resultType="java.lang.Integer">        select count(followerId) from relationship where followeeId=#{followeeId}    </select>    <select id="getFolloweeNumber" resultType="java.lang.Integer">        select count(followeeId) from relationship where followerId=#{followerId}    </select></mapper>